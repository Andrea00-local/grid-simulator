import type { LevelConfig } from '../types'

export const LEVEL1_CONFIG: LevelConfig = {
  level: 1,
  title: 'Bilancio Nazionale Annuale',
  subtitle: 'Regola la capacità installata per fonte e osserva il mix energetico e le emissioni CO₂.',
  resolution: 'annual',
  activeZones: ['national'],
  hasStorage: false,
  hasTransmission: false,
}
