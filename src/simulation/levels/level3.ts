import type { LevelConfig } from '../types'

export const LEVEL3_CONFIG: LevelConfig = {
  level: 3,
  title: 'Giorno Tipo Orario',
  subtitle: 'Bilancio orario con storage — un giorno tipo lavorativo per ogni mese.',
  resolution: 'monthly',
  activeZones: ['national'],
  hasStorage: true,
  hasTransmission: false,
}
